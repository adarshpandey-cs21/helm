# Path to Claude Code's projects directory.
# Defaults to ~/.claude/projects/ if unset.
# Use an absolute path — shell expansion (e.g. ~) is not performed.
# CLAUDE_PROJECTS_DIR=/Users/yourname/.claude/projects
